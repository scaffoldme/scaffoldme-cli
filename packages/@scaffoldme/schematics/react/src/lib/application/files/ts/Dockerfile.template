FROM node:10.14


RUN mkdir /app
WORKDIR /app

COPY ./ /app

RUN npm install
RUN npm run build


# Bind to all network interfaces so that it can be mapped to the host OS
ENV HOST=0.0.0.0 PORT=3000

EXPOSE ${PORT}
CMD ["npm", "run", "serve"]
