{
  "name": "@scaffoldme/core",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "watch": "tsc -w --preserveWatchOutput",
    "build": "npm run clean && tsc",
    "lint": "tslint --project tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -Rf dist"
  },
  "dependencies": {
    "@scaffoldme/utils": "^1.0.0",
    "os-name": "3.1.0"
  },
  "devDependencies": {
    "tslint": "^5.9.1",
    "typescript": "^3.4.5"
  }
}
